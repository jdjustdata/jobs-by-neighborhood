setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
    worker: worker/Dockerfile
release:
  command: ./heroku-release-tasks.sh
  image: worker
run:
  web: gunicorn main.wsgi --log-file -
