setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
    worker: worker/Dockerfile
release:
  image: worker
  command:
    - ./heroku-release-tasks.sh
run:
  web: gunicorn main.wsgi --log-file -
