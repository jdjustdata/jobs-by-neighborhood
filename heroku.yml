setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  config:
    DJANGO_SETTINGS_MODULE: main.settings_deploy
  docker:
    web: Dockerfile
run:
  web: gunicorn main.wsgi --log-file -
