setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    DJANGO_SETTINGS_MODULE: main.settings_deploy
build:
  docker:
    web: Dockerfile
run:
  web: gunicorn main.wsgi --log-file -
